riot.mount('div#app', 'plugandwork')

/*ipcRenderer.on('refresh', function(e, state) {
  console.log(state)
  if (state && state != "unset"){
    riot.mount('div#app', 'plugandwork-main')
  } else {
    riot.mount('div#app', 'plugandwork-login-form')
  }
})
*/
